export default function add(x, y) {
  return x + y;
}

export function subtract(x, y) {
  return x - y;
}

// export { add, subtract };

//if we have only one function to export then we use export default
// export default add;

//if we are not using default keyword then we have to use { } in import 