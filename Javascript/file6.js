//objects

const Student = {
  name: "Amit",
  age: 21,
};
console.log(Student);
console.log(Student.name);
console.log(Student.age);

Student.city = "Faridabad"; //create new property name city
// console.log(Student);

// Student.city = "Ballabgarh"; //it will override the city property because it is already present
// console.log(Student);

// console.log(Student.name);
// console.log(Student["name"]);

// const keys = Object.keys(Student);
// console.log(keys);

// const values = Object.values(Student);
// console.log(values);

